# Introduction

A character level GPT model trained on French sonnet corpus.

# Usage

- Full model architecture in `model.py`
- Use `train.py` to run training loops
- Use `generate.py` to test generation 
